[project]
name = "bootlegger-server"
version = "0.1.0"
description = "Bootlegger: personal Sleeper autopilot (storefront + back room)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools]
packages = { find = { include = ["app*", "hands*"] } }
