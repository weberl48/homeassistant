# ARMED 2026-08-25 — the live workflow is the canonical copy at
# .github/workflows/lineup-check.yml (repo root). This file is a pointer so
# the ops directory still documents that the safety net exists; edit the
# armed copy, not this one. It needs repo variables SLEEPER_LEAGUE_ID and
# SLEEPER_ROSTER_ID (Settings → Secrets and variables → Actions → Variables).
